# Módulo B – Desarrollo Frontend (Recrear interfaz)

🔙 [Volver al README principal](../README.md)

Este módulo tuvo como objetivo evaluar la **capacidad del competidor para interpretar y reproducir una interfaz gráfica a partir de un diseño de referencia**, utilizando exclusivamente **HTML y CSS**, sin apoyo de frameworks, preprocesadores o JavaScript.

La prueba buscaba medir la **precisión visual**, la **organización del código** y el **dominio del modelo de cajas, posicionamiento y estilos responsivos**. Además, se valoraba la correcta estructura semántica del HTML y la fidelidad del resultado final respecto al diseño original.

---

## Desempeño Personal

Logré **completar por completo la interfaz solicitada**, incorporando incluso **mejoras adicionales que aportaron un factor diferencial** frente a la versión base.

Entre las optimizaciones que implementé se incluyen:

-   Mejora de la **accesibilidad**, añadiendo navegación mediante teclado y etiquetas `aria-label` para describir elementos interactivos.
-   Aplicación de **buenas prácticas SEO**, como el uso adecuado de jerarquías de encabezados y texto alternativo en imágenes.
-   Optimización del **manejo de imágenes** para mejorar el rendimiento y la carga visual del sitio.

A pesar de estos logros, se me descontaron algunos puntos por dos aspectos específicos:

1. Las **cards de una sección no contenían enlaces (`href`)**, por lo que no redirigían al hacer clic.
2. Apliqué un **ancho máximo de 1200px** para mantener la legibilidad en pantallas grandes; sin embargo, esto provocó que el diseño no coincidiera exactamente con el solicitado en monitores de mayor resolución.

En general, el resultado final destacó por su **fidelidad visual, estructura semántica sólida y enfoque en accesibilidad y experiencia de usuario**, reflejando un dominio completo de las bases de maquetación moderna.

---

## Ejecutar el proyecto

1. Abre la carpeta `/solution` y abrir el archivo `index.html` en un navegador
