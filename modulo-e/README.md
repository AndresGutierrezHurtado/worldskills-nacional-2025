# Módulo E – Integración Frontend con Consumo de API

🔙 [Volver al README principal](../README.md)

En este módulo se debía desarrollar una **interfaz web adaptada a mobile que consumiera datos desde una API**, evaluando tanto la parte visual como la capacidad de integración y manejo dinámico del DOM.

---

## Desempeño Personal

Llegué a este módulo con una **preparación centrada en React**, sin embargo, durante la competencia **no se permitió el uso de React Router DOM**.  
Para no perder tiempo configurando manualmente un sistema de enrutamiento desde cero, **opté por adaptar mi estrategia** y desarrollar la solución utilizando **HTML y CSS puros**, junto con **JavaScript** para la manipulación del DOM y la realización de peticiones a la API.

A pesar de este cambio de enfoque, logré **completar alrededor del 90% de la prueba**, implementando todas las interfaces y la mayoría de las funcionalidades principales.  
Solo quedó pendiente la sección con **mayor ponderación en puntaje**, pero prioricé entregar una solución **estable, funcional y visualmente coherente** dentro del tiempo establecido.

El resultado final fue muy positivo: **recreé con precisión la interfaz solicitada**, demostrando **adaptabilidad, dominio del frontend y capacidad para resolver imprevistos de manera efectiva bajo presión**.

---

## Ejecutar el proyecto

1. Abre la carpeta `/solution`.
2. Ejecuta el archivo `index.html` en un navegador web.
3. Interactúa con la interfaz para visualizar el consumo de datos y el manejo dinámico del DOM.
